---
- name: Remove only explicitly authorized relay-owned qualification credential state
  hosts: relay
  become: true
  gather_facts: true
  roles:
    - role: relay_preflight
    - role: relay_rollback
    - role: relay_credential_cleanup
